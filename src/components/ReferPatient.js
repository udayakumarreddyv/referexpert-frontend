import { useState } from 'react';

function ReferPatient({ classes }) {
    return (
        <section id='referPatient-container'>
            Put refer patient content here
        </section>
    );
};

export default ReferPatient;